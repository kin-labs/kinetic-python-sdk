[tool.poetry]
name = "kinetic-sdk"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"
openapi-client = {path = "src/generated"}
solana = "^0.25.1"
pybase64 = "^1.2.2"
solders = "0.2.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
